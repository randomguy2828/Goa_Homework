def manual_sum(numbers):
    total = 0
    for num in numbers:
        total += num
    return total

def manual_len(iterable):
    count = 0
    for _ in iterable:
        count += 1
    return count
